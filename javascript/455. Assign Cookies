var findContentChildren = function(g, s) {

    g.sort(function(a, b) { return a - b})
    s.sort(function (a, b) { return a - b })
    var iResult = 0;

    s.forEach((element) => { 
        if (element >= g[j]) { 
            iResult++
        }
    })
    return iResult  
};
